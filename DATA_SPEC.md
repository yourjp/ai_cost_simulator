# AI 가격 대시보드 데이터 작성 규격 (DATA_SPEC.md)

이 문서는 `data.md`를 새로 만들거나 최신화할 때 따르는 단독 작성 규격이다. 기존 `data.md` 예시를 보지 않아도 이 문서만으로 OpenAI, Anthropic, Google 3개 회사의 최신 API 모델을 조사하고, 가격, 성능 스펙, 출시/가격 변경 뉴스를 대시보드가 안정적으로 파싱할 수 있는 Markdown 표로 작성해야 한다.

마지막 점검 기준일: 2026-07-31

---

## 핵심 원칙

1. `data.md`의 3개 표 헤더는 절대 바꾸지 않는다. 대시보드 스크립트가 헤더 이름과 순서로 데이터를 읽는다.
2. 표 안의 값에는 파이프 문자(`|`)를 넣지 않는다. 설명이 길면 쉼표나 괄호로 나눈다.
3. 가격은 USD 기준 `1M tokens` 단가로만 적는다. `$`, `,`, `per MTok` 같은 단위 표기는 제거하고 숫자만 쓴다.
4. 모델명은 공급사 공식 문서의 API 모델명 또는 공식 제품명을 우선한다. 임의 번역, 별칭, 루머 모델명은 쓰지 않는다.
5. 가격, 컨텍스트, 최대 출력 토큰, 출시일은 변동이 잦으므로 매번 공식 문서에서 재확인한다.
6. 공식 문서와 뉴스가 충돌하면 공식 문서를 우선하고, 뉴스는 타임라인 설명에만 보조로 사용한다.
7. 확인 불가능한 벤치마크는 `-`로 둔다. 추정치나 블로그 숫자를 사실처럼 넣지 않는다.
8. 무료 티어, Batch, Flex, Priority, 캐시 할인, 장기 컨텍스트 할증처럼 조건부 가격은 기본 표에는 넣지 말고 타임라인 또는 설명에만 간단히 적는다.
9. 기본 가격표에는 일반 API의 Standard 또는 기본 텍스트 입출력 가격만 사용한다. 이미지 생성, 음성 분당 과금, 검색 grounding, 캐시 저장료, Batch/Flex/Priority 가격은 제외한다.
10. `data.md`를 업데이트할 때는 이 문서의 표 제목과 헤더 문구를 그대로 사용한다.
11. 새 데이터셋 파일을 생성할 때는 작성 날짜를 파일명에 포함한 반영본을 함께 만든다. 파일명은 `data-YY-MM-DD.md` 형식을 사용한다.
12. 날짜별 반영본은 과거 가격과 조사 결과를 보존하는 불변 스냅샷으로 취급하며, 이미 존재하는 날짜 파일은 덮어쓰지 않는다.
13. AI 모델 비교 데이터는 모델별 출시일 또는 마지막 일반 제공 확인일로부터 1년간 보관한다. 1년이 경과한 예전 모델은 최신 가격·스펙 비교표와 선택 후보에서 삭제한다.
14. 1년 경과 모델을 삭제할 때도 날짜별 스냅샷과 뉴스 타임라인의 과거 기록은 보존하고, 삭제·교체 사실을 뉴스 또는 작업 요약에 남긴다.
12. 대시보드 파서는 최신 반영본 `data.md`를 읽으므로, 날짜 포함 반영본을 만든 뒤 같은 내용을 `data.md`에도 반영한다.
13. 이 문서 안의 placeholder 행, 예시 행, 과거 모델명은 절대 `data.md`에 복사하지 않는다. 조사 시점의 공식 문서에서 확인한 실제 모델만 작성한다.
14. 웹 검색이나 공식 문서 접속이 불가능하면 최신화 작업을 완료한 척하지 말고, 최신 가격 확인이 불가능하다고 명시한다.

---

## 파일명 규칙

- 최신 대시보드 반영본은 항상 `data.md`로 저장한다.
- 날짜별 반영본은 `data-YY-MM-DD.md` 형식으로 저장한다. 예: `data-26-07-31.md`
- 새로 조사해 데이터셋을 생성하거나 최신화할 때는 날짜별 반영본과 `data.md`의 내용이 같아야 한다.
- `데이터 업데이트: <YYYY.MM.DD>`의 날짜와 반영본 파일명의 `YY-MM-DD`는 같은 날짜를 가리켜야 한다.
- `scripts/parse-data.js`는 `data.md`를 읽으므로 검증 전에는 반드시 최신 내용을 `data.md`에 반영한다.

---

## data.md 최종 파일 구조

`data.md`와 `data-YY-MM-DD.md`는 아래 구조만 사용한다. 기존 `data.md` 내용을 참고하지 못하는 상황에서도 이 골격으로 새 파일을 만들 수 있어야 한다.

```markdown
# AI 가격 시뮬레이터 실거래 데이터셋 (data.md)

이 파일의 테이블 내용을 수정하고 빌드하면 대시보드 시뮬레이터에 실시간 반영됩니다.
최신화할 때는 `DATA_SPEC.md`의 공식 출처, 가격 기준, 표 헤더 규격을 먼저 따른다.
데이터 업데이트: <YYYY.MM.DD>

---

### 1. 모델 요금 테이블
| 제공사 | 모델명 | 등급 (high/mid) | 입력 요금 ($/1M) | 출력 요금 ($/1M) |
| :--- | :--- | :--- | :--- | :--- |
| <OpenAI 또는 Anthropic 또는 Google> | <공식 모델명> | <high 또는 mid> | <숫자만> | <숫자만> |

---

### 2. 모델 성능 스펙 테이블
| 모델명 | 컨텍스트 | 출력 한도 | MMLU-Pro | SWE-bench Pro | 핵심 특장점 및 권장 용도 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| <1번 표와 정확히 같은 모델명> | <예: 1M> | <예: 128k> | <예: 91.2%> | <예: 41.2%> | <확인된 장점과 권장 용도 한 문장> |

---

### 3. 뉴스 타임라인 테이블
| 제공사 | 날짜 | 헤드라인 | 세부 내용 (요금 괄호 표기 필수) |
| :--- | :--- | :--- | :--- |
| <OpenAI 또는 Anthropic 또는 Google> | <YYYY-MM-DD> | <출시 또는 가격 변경 요약> | <모델명과 요금 괄호 포함, 예: (입력 $1.00/출력 $8.00)> |

---

### 4. 최근 1년 가격 추이 보관 테이블
| 등급 (high/mid) | 기준일 | OpenAI 입력 요금 ($/1M) | Anthropic 입력 요금 ($/1M) | Google 입력 요금 ($/1M) |
| :--- | :--- | :--- | :--- | :--- |
| <high 또는 mid> | <YYYY-MM-DD> | <숫자만> | <숫자만> | <숫자만> |
```

작성할 때는 `<...>` placeholder 행을 모두 삭제하고 실제 조사 결과 행만 남긴다.

`데이터 업데이트`는 조사와 `data.md` 작성이 끝난 날짜를 `YYYY.MM.DD` 형식으로 적는다. 앱 최상단에는 이 값이 `데이터 업데이트: ...`로 표시된다.

---

## 조사 대상 회사와 우선 모델

`data.md`에는 반드시 아래 3개 회사만 사용한다.

- `OpenAI`
- `Anthropic`
- `Google`

각 회사별로 최소 2개 이상, 가능하면 `high` 모델 1개 이상과 선택 비교용 `mid` 모델 2개 정도를 함께 조사한다.

권장 우선순위:

- OpenAI: 공식 가격표의 최신 GPT 주력 모델군에서 최고 성능 모델, 균형형 모델, 저가형 모델을 확인한다.
- Anthropic: 공식 가격표의 최신 Claude 주력 모델군에서 최고 성능 모델, 균형형 모델, 저가형 모델을 확인한다.
- Google: Gemini Developer API 가격표의 최신 Gemini 모델군에서 Pro, Flash, Flash-Lite 등 성능/가격 포지션이 다른 모델을 확인한다.
- Google Pro 계열은 최신 Pro 모델이 공식 가격표에 없더라도, 직전 일반 제공 Pro 모델이 확인되면 high 모델 비교 후보에 포함한다.

모델 후보는 `DATA_SPEC.md`에 적힌 이름이 아니라 작성 시점의 공식 가격표와 모델 문서를 기준으로 고른다.

---

## 권장 공식 출처

아래 출처를 먼저 확인한다.

- OpenAI 가격: https://developers.openai.com/api/docs/pricing
- OpenAI 모델/출시 공지: https://openai.com/news 또는 https://openai.com/index
- Anthropic 가격: https://platform.claude.com/docs/en/about-claude/pricing
- Anthropic 모델 개요: https://platform.claude.com/docs/en/about-claude/models/overview
- Anthropic 뉴스: https://www.anthropic.com/news
- Google Gemini 가격: https://ai.google.dev/gemini-api/docs/pricing
- Google Gemini 모델/가이드: https://ai.google.dev/gemini-api/docs/models 또는 https://ai.google.dev/gemini-api/docs/gemini-3
- Google DeepMind 모델 페이지: https://deepmind.google/models

보조 출처는 공식 문서에 없는 출시 맥락, 가격 변경 배경, 비교 설명을 확인할 때만 사용한다. 보조 출처를 사용했다면 타임라인의 `세부 내용`에 공식 문서에서 확인된 가격을 반드시 함께 적는다.

---

## 조사 절차

1. 각 회사의 공식 가격 페이지를 열고 최신 일반 제공 API 모델을 확인한다.
2. 모델별 기본 입력/출력 가격을 `$/1M tokens`로 정규화한다.
3. 같은 모델에 short context와 long context 가격이 따로 있으면 대시보드 비교 기준을 하나로 고정한다.
   - 기본값: 일반 API 기본, Standard, short/default context 가격.
   - 장기 컨텍스트 가격은 `data.md` 기본 가격표에 쓰지 않는다. 꼭 필요하면 타임라인의 세부 내용에만 조건부 가격으로 적는다.
4. 컨텍스트 윈도우와 최대 출력 토큰을 공식 모델 문서에서 확인한다.
5. Vals Index 같은 벤치마크는 Vals AI 또는 신뢰 가능한 벤치마크 표에서 확인한다.
6. 출시일, 가격 인하, 모델 교체, deprecated/retired 상태를 타임라인에 기록한다.
7. 작성 날짜를 파일명에 포함한 `data-YY-MM-DD.md` 반영본을 저장한다.
8. 같은 내용을 최신 반영본 `data.md`에 저장한 뒤 표 헤더, 열 개수, 숫자 형식이 깨지지 않았는지 확인한다.
9. `scripts/parse-data.js`를 실행할 수 있는 환경이면 `node scripts/parse-data.js`로 파싱 검증을 수행한다.

---

## 모델 수명주기 변경 처리 규칙

`data.md`는 현재 대시보드 비교용 데이터셋이다. 가격표와 스펙표에는 현재 일반 제공되어 비교할 모델만 남기고, 과거 변경 이력은 뉴스 타임라인에 기록한다.

### 신규 모델이 생긴 경우

- 공식 가격표와 모델 문서에 새 모델이 일반 제공 상태로 올라왔으면 1번 요금 테이블과 2번 스펙 테이블에 새 행을 추가한다.
- 새 모델이 기존 모델보다 같은 역할에서 더 최신이면 최신 모델을 우선한다. 예를 들어 새 flagship이 나오면 이전 flagship은 계속 일반 제공 중인지 확인한 뒤 유지 또는 제거를 결정한다.
- preview, beta, limited availability, waitlist 모델은 기본적으로 가격/스펙 테이블에 넣지 않는다. 단, 가격과 사용 가능 범위가 공식 문서에 명확하고 대시보드 비교 가치가 높으면 넣을 수 있으며, 특장점에 `preview` 상태를 짧게 표시한다.
- 타임라인에는 새 모델 출시 또는 일반 제공 전환을 최신순으로 추가한다. 세부 내용에는 반드시 요금 괄호를 넣는다.

### 기존 모델이 없어지거나 deprecated/retired 된 경우

- 공식 문서에서 retired, deprecated, legacy, unavailable, removed, replacement 권고가 확인되면 1번 요금 테이블과 2번 스펙 테이블에서 제거한다.
- 제거한 모델의 이력은 3번 뉴스 타임라인에 남긴다.
- 대체 모델이 공식 문서에 명시되어 있으면 타임라인 세부 내용에 `대체 모델: <모델명>`을 포함한다.
- 공식 가격표에서 사라졌지만 deprecation 공지가 없으면 삭제하기 전에 모델 문서, changelog, 뉴스 페이지를 한 번 더 확인한다. 그래도 확인이 안 되면 가격/스펙 표에서는 제거하고 타임라인에 `공식 가격표에서 제외 확인`이라고 적는다.

### 후속 버전 또는 이름 변경이 생긴 경우

- 버전 번호, 날짜 suffix, 제품명이 바뀐 모델은 새 공식 모델명으로 별도 모델처럼 취급한다.
- 이전 버전이 계속 일반 제공 중이고 비교 가치가 있으면 유지할 수 있다. 다만 같은 회사에서 모델 수가 과도해지면 최신 버전과 대표 저가형 모델을 우선하고 구버전은 제거한다.
- 단순 display name 변경이고 API ID가 동일하면 기존 행의 모델명을 공식 표시명 또는 API ID 기준으로 갱신하고 타임라인에 이름 변경을 기록한다.
- API ID가 바뀌었으면 1번 요금 테이블과 2번 스펙 테이블의 모델명을 새 ID 또는 새 공식 모델명으로 모두 동일하게 바꾼다.

### 가격, 컨텍스트, 출력 한도만 바뀐 경우

- 모델명은 그대로 두고 1번 요금 테이블 또는 2번 스펙 테이블의 값을 최신 공식 값으로 덮어쓴다.
- 변경 전 가격을 가격표에 함께 남기지 않는다.
- 타임라인에는 가격 인하, 가격 인상, 컨텍스트 확대, 출력 한도 변경을 추가한다. 세부 내용에는 최신 요금 괄호를 넣고, 확인 가능하면 이전 요금도 짧게 적는다.

### 모델 목록 균형 규칙

- 각 회사별 최소 2개 모델을 유지한다.
- 한 회사에 모델이 너무 많아지면 `high` 대표 1~2개와 `mid` 대표 2개 정도를 우선한다.
- 같은 역할의 구버전과 신버전이 동시에 있으면 신버전을 우선한다.
- 단, 최신 역할 모델이 가격표나 일반 제공 API 목록에 없으면 같은 역할의 직전 일반 제공 모델을 유지한다. 예: Google 최신 Pro가 없으면 이전 Pro 버전을 high 후보로 표시한다.
- 현재 일반 제공 모델이 회사별 2개 미만이면 확인 가능한 모델만 넣고, 부족한 이유를 타임라인 또는 작업 요약에 명시한다.

---

## 1. 모델 요금 테이블 (Model Pricing)

헤더 규격:

```markdown
| 제공사 | 모델명 | 등급 (high/mid) | 입력 요금 ($/1M) | 출력 요금 ($/1M) |
```

작성 규칙:

- `제공사`: `Google`, `OpenAI`, `Anthropic` 중 하나만 쓴다.
- `모델명`: 공식 모델명 또는 대시보드에 표시할 공식 제품명을 쓴다.
- `등급 (high/mid)`:
  - `high`: flagship, 최고 성능, 고난도 추론/코딩/장문 작업용.
  - `mid`: 속도/가격 효율, 대량 처리, 일상 작업용.
- `입력 요금`, `출력 요금`: 1M 토큰당 USD 숫자만 쓴다. 예: `2.50`, `15.00`, `0.125`.
- 캐시 입력, batch 할인, preview 할인, storage 가격은 이 표에 넣지 않는다.

형식 예시, 복사 금지:

```markdown
### 1. 모델 요금 테이블
| 제공사 | 모델명 | 등급 (high/mid) | 입력 요금 ($/1M) | 출력 요금 ($/1M) |
| :--- | :--- | :--- | :--- | :--- |
| OpenAI | <공식 모델명> | high | 2.50 | 15.00 |
| Google | <공식 모델명> | mid | 0.10 | 0.40 |
```

---

## 2. 모델 성능 스펙 테이블 (Model Performance Specs)

이 표는 대시보드의 `최상위(High-tier) 모델 성능 비교`와 `가성비(Mid-tier) 모델 성능 비교` 영역에 직접 사용된다. 앱은 1번 요금 테이블의 `등급 (high/mid)`으로 모델을 high/mid 성능 비교 표에 나누어 표시하고, `MMLU-Pro` 값을 숫자로 파싱해 높은 순서대로 정렬한다. `SWE-bench Pro`는 실무 코딩 엔지니어링 에이전트 작업 성과를 나타내는 평가지표로 함께 표시한다.

헤더 규격:

```markdown
| 모델명 | 컨텍스트 | 출력 한도 | MMLU-Pro | SWE-bench Pro | 핵심 특장점 및 권장 용도 |
```

작성 규칙:

- `모델명`: 1번 가격 테이블의 모델명과 정확히 일치시킨다.
- `컨텍스트`: `2M`, `1M`, `0.2M`, `128k`처럼 축약 표기한다.
- `출력 한도`: `128k`, `64k`, `16k`, `8k`처럼 축약 표기한다.
- `MMLU-Pro`: 학술적 종합 지능 성능 지수를 `%`로 적는다. 이 값은 성능 비교 표 정렬에 사용되므로 확인된 값에는 숫자와 `%` 외 설명을 넣지 않는다.
- `SWE-bench Pro`: 장기 소프트웨어 엔지니어링 이슈 해결 벤치마크의 점수를 `%`로 적는다. 확인하지 못한 경우 `없음` 또는 `-`로 표시한다.
- `핵심 특장점 및 권장 용도`: 한 문장으로 쓴다. 가격, 속도, 추론, 코딩, 멀티모달, 장문 컨텍스트, 대량 처리 중 실제 확인된 장점을 적는다.
- 성능 비교 UI에는 `컨텍스트`, `출력 한도`, `MMLU-Pro`, `SWE-bench Pro`, `핵심 특장점 및 권장 용도`, 요금 테이블의 입력/출력 요금이 함께 표시된다.
- 성능 비교와 선택박스 후보가 모두 보이도록 각 회사별로 가능하면 high 1개 이상, mid 2개 정도를 포함한다.

형식 예시:

```markdown
### 2. 모델 성능 스펙 테이블
| 모델명 | 컨텍스트 | 출력 한도 | MMLU-Pro | SWE-bench Pro | 핵심 특장점 및 권장 용도 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| Claude Opus 4.6 | 1M | 128k | 92.5% | 48.2% | 독보적 코딩·추론 지능, Fast Mode 지원(속도 2.5배, 가격 6배) |
| Claude Sonnet 4.6 | 1M | 64k | 88.7% | 39.5% | 속도와 성능의 조화, 프로페셔널 코드 리팩토링 특화 |
```

---

## 3. 뉴스 타임라인 테이블 (Timeline News)

헤더 규격:

```markdown
| 제공사 | 날짜 | 헤드라인 | 세부 내용 (요금 괄호 표기 필수) |
```

작성 규칙:

- `제공사`: `Google`, `OpenAI`, `Anthropic` 중 하나만 쓴다.
- `날짜`: `YYYY-MM-DD` 형식으로 쓴다.
- `헤드라인`: 출시, 가격 인하, preview 종료, 모델 교체, retired/deprecated 등 변화를 짧게 적는다.
- `세부 내용`: 관련 모델명과 요금을 반드시 괄호로 포함한다. 예: `(입력 $2.50/출력 $15.00)`.
- 최근 6개월 안의 출시, 가격 변경, 모델 교체, deprecated/retired, 일반 제공 전환 뉴스를 우선 포함한다.
- 같은 회사에서 여러 변경이 있으면 최신순으로 적는다.
- 공식 출시일을 찾지 못하면 뉴스 발행일이 아니라 공식 문서의 업데이트 날짜를 우선한다.

형식 예시, 복사 금지:

```markdown
### 3. 뉴스 타임라인 테이블
| 제공사 | 날짜 | 헤드라인 | 세부 내용 (요금 괄호 표기 필수) |
| :--- | :--- | :--- | :--- |
| OpenAI | 2026-07-30 | <모델 출시 또는 가격 변경 요약> | <공식 문서에서 확인한 모델명과 요금, 예: (입력 $1.00/출력 $8.00)> |
| Anthropic | 2026-07-31 | <모델 출시 또는 가격 변경 요약> | <공식 문서에서 확인한 모델명과 요금, 예: (입력 $3.00/출력 $15.00)> |
```

---

## 4. 최근 1년 가격 추이 보관 테이블 (One-Year Price History)

이 표는 대시보드의 최근 3개월 가격 추이 차트에 사용할 1년 원본 이력으로 관리한다. 앱은 `등급 (high/mid)` 값으로 high 차트와 mid 차트를 나누고, `기준일`을 X축으로 사용하며, 각 회사의 입력 요금을 선 그래프로 표시한다.

헤더 규격:

```markdown
| 등급 (high/mid) | 기준일 | OpenAI 입력 요금 ($/1M) | Anthropic 입력 요금 ($/1M) | Google 입력 요금 ($/1M) |
```

작성 규칙:

- `등급 (high/mid)`: `high` 또는 `mid`만 쓴다.
- `기준일`: `YYYY-MM-DD` 형식으로 쓴다.
- 각 회사 입력 요금: 해당 날짜의 대표 high 또는 mid 모델 입력 요금을 1M 토큰당 USD 숫자만 쓴다.
- 모델별 가격 추이는 최근 1년 범위를 7일 단위로 기록·보관한다. 권장: 매주 같은 요일의 기준일과 최신 기준일.
- 최신 기준일 행은 1번 모델 요금 테이블의 현재 대표 모델 입력 요금과 일치해야 한다.
- 새 기준일 행은 기존 가격 추이 행 뒤에 추가하며, 기존 날짜 행은 삭제하거나 현재 가격으로 소급 변경하지 않는다. 화면 그래프 표시 기간은 별도 UI 규칙에 따라 최근 3개월로 제한할 수 있다.
- 모델별 가격 추이에서는 해당 모델의 출시일 이전 기준일을 표시하지 않는다. 출시일을 확인하지 못하면 모델별 과거 선을 임의로 만들지 않고 없음 또는 공백으로 표시한다.
- 과거 가격은 공식 가격표 changelog, 공식 뉴스, 가격 변경 공지, 또는 신뢰 가능한 보조 출처로 확인한다.
- 과거 가격을 찾지 못하면 해당 구간은 최신 확인 가격을 반복해서 넣고, 뉴스 타임라인 또는 작업 요약에 `과거 가격 변경 확인 불가`라고 명시한다.
- 차트는 입력 요금만 사용한다. 출력 요금 추이는 이 표에 넣지 않는다.
- 날짜는 오래된 순서에서 최신 순서로 적는다.

형식 예시, 복사 금지:

```markdown
### 4. 최근 1년 가격 추이 보관 테이블
| 등급 (high/mid) | 기준일 | OpenAI 입력 요금 ($/1M) | Anthropic 입력 요금 ($/1M) | Google 입력 요금 ($/1M) |
| :--- | :--- | :--- | :--- | :--- |
| high | 2026-07-03 | 2.50 | 3.00 | 1.25 |
| high | 2026-07-31 | 2.50 | 3.00 | 1.25 |
| mid | 2026-07-03 | 0.25 | 0.80 | 0.10 |
| mid | 2026-07-31 | 0.25 | 0.80 | 0.10 |
```

---

## 세부 조사 팁

- OpenAI 가격표는 모델별로 short context, long context, priority 가격이 나뉠 수 있다. 대시보드 목적이 일반 비교라면 short/default 가격을 쓰고, 장문 비교라면 long context 가격을 쓰되 타임라인에 기준을 남긴다.
- Anthropic 가격표는 standard와 batch 가격이 따로 있다. 기본 가격 테이블에는 standard 가격을 쓰고 batch는 제외한다.
- Anthropic은 introductory pricing처럼 날짜가 있는 임시 가격이 있을 수 있다. 기준일 현재 적용되는 가격을 가격표에 넣고, 종료일과 표준 가격은 타임라인에 적는다.
- Google Gemini 가격표는 모델, 모달리티, tier, batch, live API가 섞여 있다. 텍스트/일반 API 기준 가격을 우선하고 이미지 생성, 음성 분당 과금, grounding 비용은 제외한다.
- Google 모델은 `Pro`, `Flash`, `Flash-Lite`가 역할이 다르다. 단순히 숫자가 높다고 high로 넣지 말고 성능 포지션과 가격 포지션을 함께 본다.
- Google Pro 계열은 문서작성, 코딩, 장문 추론 비교에서 의미가 크므로 최신 Pro가 없으면 이전 일반 제공 Pro를 남기고 특장점에 이전 세대임을 표시한다.
- Vals Index 또는 SWE-bench Pro가 없는 모델도 성능 비교 후보에 유지하고 `없음`으로 표시한다.
- preview, beta, limited availability 모델은 실제 API 사용 가능 여부를 확인한다. 제한 모델은 타임라인에는 넣을 수 있지만 가격/성능 표에는 일반적으로 제외한다.
- retired, deprecated, legacy 모델은 비교 목적이 아니라면 삭제한다. 삭제 시 타임라인에 교체 모델을 기록한다.
- 모델 보관 기간이 1년을 초과하면 retired/deprecated 여부와 관계없이 현재 비교 데이터에서 제거하고, 과거 스냅샷과 삭제 이력만 보존한다.
- 동일 모델의 display name과 API ID가 다르면 가격 테이블에는 API ID를 우선하고, 특장점에 display name을 언급한다.
- `data.md`에 넣기 전 회사별로 최소 2개 공식 출처를 교차 확인한다. 가격 페이지 1개, 모델/뉴스 페이지 1개가 가장 좋다.

---

## AI 업데이트 프롬프트 예시

AI에게 `DATA_SPEC.md`만 전달해 `data.md`를 최신화시키려면 아래 프롬프트를 그대로 사용한다.

```text
DATA_SPEC.md 규격을 엄격히 따라 data.md를 최신화해줘.

요구사항:
1. OpenAI, Anthropic, Google 3개 회사의 최신 일반 제공 API 모델을 공식 문서 기준으로 조사한다.
2. 각 회사별로 최소 2개 모델을 넣고, 가능하면 high 1개 이상과 mid 2개 정도가 보이게 구성한다.
3. 가격표에는 일반 API의 Standard 또는 기본 텍스트 입출력 가격만 숫자로 넣는다.
4. Batch, Flex, Priority, 캐시, long context 할증, 이미지 생성, 음성 분당 과금, grounding 비용은 가격표에서 제외한다.
5. 컨텍스트, 출력 한도, Vals Index, SWE-bench Verified는 공식 문서 또는 신뢰 가능한 출처로 확인한다. 확인하지 못한 지표는 `없음`으로 두고 모델은 그대로 표시한다.
6. 모델명은 1번 요금 테이블과 2번 스펙 테이블에서 정확히 같아야 한다.
7. 신규 모델, deprecated/retired 모델, 후속 버전, 이름 변경, 가격/컨텍스트 변경은 `모델 수명주기 변경 처리 규칙`에 따라 반영한다.
8. 4번 가격 추이 테이블은 high/mid 각각 최근 1년의 약 52~53개 기준일을 7일 단위로 오래된 순서에서 최신 순서로 작성한다.
9. 3번 뉴스 타임라인은 최근 6개월간의 주요 뉴스와 가격 변경 이력을 최신순으로 작성하고, 세부 내용에는 반드시 `(입력 $x.xx/출력 $y.yy)` 형식의 요금 괄호를 넣는다.
10. data.md의 표 제목과 헤더는 DATA_SPEC.md에 있는 문구와 정확히 동일하게 유지한다.
11. 작성 날짜를 파일명에 포함한 data-YY-MM-DD.md 반영본을 만들고, 같은 내용을 data.md에도 반영한다.
12. 최신 가격 조사 시 기존 날짜별 파일과 과거 가격 추이를 보존하고 새 조사일의 가격과 변동 이력만 누적한다.
12. 업데이트 후 열 개수, 가격 숫자 형식, 날짜 형식, 제공사 이름을 검증한다.
13. 조사에 사용한 공식 URL 목록을 작업 결과 요약에 함께 알려준다.
```

---

## 저장 전 검증 체크리스트

- [ ] 3개 표의 헤더가 이 문서의 헤더와 정확히 일치한다.
- [ ] 작성 날짜를 포함한 `data-YY-MM-DD.md` 반영본을 만들었다.
- [ ] `data-YY-MM-DD.md`와 최신 반영본 `data.md`의 내용이 같다.
- [ ] `데이터 업데이트: YYYY.MM.DD`와 파일명 날짜 `YY-MM-DD`가 같은 날짜다.
- [ ] 모든 행의 열 개수가 헤더와 같다.
- [ ] 제공사는 `Google`, `OpenAI`, `Anthropic`만 사용했다.
- [ ] 가격 칸에는 숫자만 있다.
- [ ] 모델명은 가격 테이블과 스펙 테이블에서 정확히 일치한다.
- [ ] 날짜는 모두 `YYYY-MM-DD` 형식이다.
- [ ] 타임라인 세부 내용에는 요금 괄호가 들어 있다.
- [ ] 뉴스 타임라인은 최근 6개월간의 주요 뉴스와 가격 변경 이력을 포함한다.
- [ ] 최근 1년 가격 추이 테이블은 high와 mid를 모두 포함하고 7일 단위 기준일로 누적 보관했다.
- [ ] 가격 추이의 최신 기준일 행은 1번 모델 요금 테이블의 현재 대표 입력 요금과 일치한다.
- [ ] 최신 가격은 공식 가격 페이지에서 다시 확인했다.
- [ ] 기존 날짜별 스냅샷을 덮어쓰지 않고 새 조사일 파일을 추가했다.
- [ ] 모델별 출시일 또는 마지막 일반 제공 확인일을 기준으로 1년 초과 모델을 현재 비교표에서 제거했다.
- [ ] 삭제한 모델의 과거 스냅샷과 교체·삭제 이력을 보존했다.
- [ ] 과거 가격 추이 행을 삭제하거나 현재 가격으로 소급 변경하지 않았다.
- [ ] 확인 불가 값은 추정하지 않고 `-`로 남겼다.
- [ ] 신규 모델은 가격/스펙 표에 추가하고 출시 이력을 타임라인에 기록했다.
- [ ] deprecated, retired, legacy, removed 모델은 가격/스펙 표에서 제거하고 대체 또는 제거 이력을 타임라인에 기록했다.
- [ ] 후속 버전 또는 이름 변경은 공식 모델명 기준으로 반영하고 이전 버전 유지 여부를 판단했다.
- [ ] 가격, 컨텍스트, 출력 한도 변경은 최신 값으로 덮어쓰고 변경 이력을 타임라인에 기록했다.
